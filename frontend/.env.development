# App base URL (used in <base href> for Android WebView asset resolution)
VITE_APP_BASE_URL=/

# Vite dev-server proxy target — the PHP backend URL
VITE_API_TARGET=http://localhost/playbook/backend

# Base path for axios in development (proxied via Vite)
VITE_API_BASE=/api
